[project]
name = "minesweeper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pillow>=11.3.0"]

[tool.uv]
dev-dependencies = ["pyinstaller>=6.14.2"]
